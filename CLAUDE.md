# Commit messages

- One line, imperative mood, max 72 characters.
- No body, no `Co-Authored-By` trailers, no other trailers.

Examples:

- `Add gitalong update with local-only revwalk`
- `Fix is_file_tracked to use HEAD tree lookup`
- `Rewrite README and add Rust CI workflow`
