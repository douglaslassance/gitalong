{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug gitalong",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": ["build", "--bin=gitalong"],
                "filter": { "name": "gitalong", "kind": "bin" }
            },
            "cwd": "${workspaceFolder}",
            "args": ["${command:pickArgs}"]
        }
    ]
}
